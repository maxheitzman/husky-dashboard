flask>=3.0.0
flask-cors>=4.0.0
numpy>=1.26.0
pandas>=2.0.0
matplotlib>=3.7.0
scikit-learn>=1.3.0

# ROS bag parsing (optional - install if you have ROS bag files)
# rosbag  # For ROS1 - install via: pip install rosbag
rosbags  # For ROS2 - install via: pip install rosbags

# GPS Prediction Model (optional - install if you want GPS predictions)
# torch  # Install via: pip install torch (or pip install torch --index-url https://download.pytorch.org/whl/cpu for CPU-only)
# joblib  # For model serialization

# CAN bus parsing (optional - install if you have CAN log files)
# python-can  # Install via: pip install python-can

